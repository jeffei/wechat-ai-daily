name: Daily WeChat AI News

on:
  schedule:
    # 每天北京时间早上 08:00 (UTC 00:00) 自动触发
    - cron: '0 0 * * *'
  workflow_dispatch: # 支持在 GitHub 后台手动点击运行

permissions:
  contents: write

jobs:
  run-daily-news:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      - name: Fetch AI News & Generate Article via Gemini
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          SERVERCHAN_KEY: ${{ secrets.SERVERCHAN_KEY }} # 可选，用于微信推送提醒
        run: |
          python src/main.py

      - name: Commit and Push Updated Article
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/ index.html
          git diff --staged --quiet || (git commit -m "Auto-update daily AI news [$(date +'%Y-%m-%d')]" && git push)
